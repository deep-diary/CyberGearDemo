# Flash 页面结构和写入限制说明

## Flash 基本特性

### 1. 页面大小

- **STM32G4**: 每页 2KB (2048 字节)
- **页面编号**: 0-63 (共 64 页)
- **总容量**: 128KB Flash

### 2. 写入限制

- **擦除**: 必须以页面为单位 (2KB)
- **写入**: 可以按字节写入，但必须先擦除整个页面
- **不能**: 单独修改页面中的某个字节而不影响其他数据

## 当前实现的影响

### 页面结构

```
页面 62 (ADDR_FLASH_PAGE_62 = 0x0801F800)
┌─────────────────────────────────────────────────────────┐
│ 0x0801F800: 页面开始                                    │
├─────────────────────────────────────────────────────────┤
│ 0x0801F800-0x0801FFE7: 其他数据 (2032字节)              │
├─────────────────────────────────────────────────────────┤
│ 0x0801FFE8: CAN ID 数据 (8字节) ← 我们的数据            │
└─────────────────────────────────────────────────────────┘
```

### 每次保存 CAN ID 时发生什么

1. **擦除整个页面** (2KB 全部变成 0xFF)
2. **写入 CAN ID 数据** (只写入 8 字节)
3. **其他数据丢失** (页面中其他 2032 字节的数据被清空)

## 解决方案对比

### 方案 1: 使用页面末尾 (当前实现)

**优点**:

- 减少与其他数据的冲突
- 实现简单

**缺点**:

- 仍然会擦除整个页面
- 页面中其他数据会丢失

### 方案 2: 使用专用页面

**优点**:

- 完全独立，不影响其他数据
- 可以存储更多配置信息

**缺点**:

- 占用一个完整的 2KB 页面
- 如果只有 8 字节数据，浪费空间

### 方案 3: 批量数据管理

**优点**:

- 最大化利用页面空间
- 减少擦除次数

**缺点**:

- 实现复杂
- 需要管理多个配置项

## 推荐实现

### 当前方案 (页面末尾)

```c
#define CAN_CONFIG_PAGE_ADDR (ADDR_FLASH_PAGE_62 + 2040)  // 页面最后8字节

// 优点: 简单，减少冲突
// 缺点: 仍然会擦除整个页面
```

### 改进方案 (专用页面)

```c
#define CONFIG_PAGE_ADDR ADDR_FLASH_PAGE_63  // 使用最后一页

// 存储多个配置项
typedef struct {
    uint8_t can_id;
    uint8_t master_id;
    uint16_t timeout;
    uint32_t reserved;
} SystemConfig_t;

// 优点: 独立页面，可以存储更多配置
// 缺点: 占用一个完整页面
```

## 实际建议

### 1. 当前项目

- 如果页面 62 没有其他重要数据，当前方案可行
- 将 CAN ID 放在页面末尾，减少冲突

### 2. 未来扩展

- 如果需要存储更多配置，考虑使用专用页面
- 实现批量配置管理

### 3. 注意事项

- **Flash 寿命**: 每次擦除都会消耗 Flash 寿命
- **数据备份**: 重要数据需要备份到其他位置
- **写入频率**: 尽量减少配置更改频率

## 测试验证

### 检查页面内容

```c
void CheckPageContent(void) {
    uint32_t pageStart = ADDR_FLASH_PAGE_62;

    printf("页面62内容:\n");
    for (int i = 0; i < 8; i++) {
        uint32_t* addr = (uint32_t*)(pageStart + i * 4);
        printf("0x%08X: 0x%08X\n", (uint32_t)addr, *addr);
    }
}
```

### 验证数据完整性

```c
void VerifyDataIntegrity(void) {
    // 保存前检查页面内容
    uint64_t beforeData = Flash_Read64BitData(CAN_CONFIG_PAGE_ADDR);

    // 保存CAN ID
    CAN_SaveIdToFlash(0x10);

    // 保存后检查页面内容
    uint64_t afterData = Flash_Read64BitData(CAN_CONFIG_PAGE_ADDR);

    printf("保存前: 0x%016llX\n", beforeData);
    printf("保存后: 0x%016llX\n", afterData);
}
```

## 总结

Flash 的页面级擦除特性意味着：

- ✅ 可以写入任意大小的数据
- ❌ 但必须先擦除整个 2KB 页面
- ⚠️ 页面中其他数据会丢失

当前实现将 CAN ID 放在页面末尾是一个合理的折中方案，既简单又减少了数据冲突的风险。
